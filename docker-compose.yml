version: '3'
services:
    backend:
        build:
            context: backend
            dockerfile: Dockerfile
        depends_on:
            - database
    frontend:
        build:
            context: frontend
            dockerfile: Dockerfile
        ports:
            - "80:80"
            - "443:443"
        volumes:
            - ./cert.crt:/etc/nginx/ssl/cert.crt
            - ./cert.key:/etc/nginx/ssl/cert.key
    database:
        build:
            context: .
            dockerfile: mariaDB/Dockerfile
        restart: always
        environment:
            MYSQL_ROOT_PASSWORD: keineGuteIdeedasHierReinZuSchreiben
            MYSQL_DATABASE: db_notes
            MYSQL_USER: admin
            MYSQL_PASSWORD: admin
        ports:
            - "3306:3306"